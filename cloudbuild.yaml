steps:

- id: "Rodando app test"
  name: 'gcr.io/cloud-builders/go'
  args: ['test', './src']

- id: "Image Build"
  name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'aguinelo_desafioci_app', '.']

images:
  - 'gcr.io/$PROJECT_ID/aguinelo_desafioci_app:latest'
